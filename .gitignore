# Dependencies — each project manages its own node_modules
node_modules/

# Local editor/agent tool state (personal, not shared)
.qodo/
.claude/settings.local.json

# Env files at any level
.env
.env.*
!.env.example
